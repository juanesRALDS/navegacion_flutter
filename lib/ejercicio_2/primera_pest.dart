import 'package:flutter/material.dart';

class PrimeraPestana extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Primera Pestaña'),
    );
  }
}
