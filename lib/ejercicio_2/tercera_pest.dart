import 'package:flutter/material.dart';

class TerceraPestana extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Tercera Pestaña'),
    );
  }
}
